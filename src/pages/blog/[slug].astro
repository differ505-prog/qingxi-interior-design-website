---
import BaseLayout from '../../layouts/BaseLayout.astro';

export function getStaticPaths() {
  return [
    { params: { slug: '2025-interior-design-trends' } },
    { params: { slug: 'small-space-tips' } },
    { params: { slug: 'budget-guide' } }
  ];
}

const { slug } = Astro.params;

const postData = {
  '2025-interior-design-trends': {
    title: '2025 室內設計趨勢',
    description: '探索今年最熱門的室內設計風格與配色',
    date: '2025-11-01',
    content: '今年的室內設計趨勢著重於永續性與自然元素。大地色系、環保材料、以及將室內外空間融合的設計手法都是熱門選擇。此外，多功能空間的概念也越來越受到重視，特別是在後疫情時代，人們需要在家中打造既能工作又能放鬆的環境。'
  }
};

const post = postData[slug] || postData['2025-interior-design-trends'];
---

<BaseLayout title={post.title}>

  <article class="container mx-auto px-4 py-16 max-w-3xl">
    <h1 class="text-4xl font-bold mb-4">{post.title}</h1>
    <p class="text-gray-500 mb-8">{post.date}</p>
    <div class="prose prose-lg max-w-none">
      <p class="text-gray-800 leading-relaxed">{post.content}</p>
    </div>
  </article>
</BaseLayout>