---
// 臨時調試頁面 - 檢查環境變數
const check = {
    public_preview_protection:
        import.meta.env.PUBLIC_PREVIEW_PROTECTION_ENABLED || "undefined",
    public_preview_password: import.meta.env.PUBLIC_PREVIEW_PASSWORD
        ? "SET"
        : "NOT SET",
    final_enabled:
        import.meta.env.PUBLIC_PREVIEW_PROTECTION_ENABLED === "true"
            ? "YES"
            : "NO",
};
---

<!doctype html>
<html>
    <head>
        <title>Environment Check</title>
        <style>
            body {
                font-family: monospace;
                padding: 2rem;
            }
            pre {
                background: #f4f4f4;
                padding: 1rem;
                border-radius: 8px;
            }
        </style>
    </head>
    <body>
        <h1>Environment Variables Debug</h1>
        <pre>{JSON.stringify(check, null, 2)}</pre>
    </body>
</html>
