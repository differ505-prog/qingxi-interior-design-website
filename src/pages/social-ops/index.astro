---
import BaseLayout from "../../layouts/BaseLayout.astro";

const defaultGroups = [
  {
    id: "group-1654316948220005",
    alias: "主社團 01",
    groupId: "1654316948220005",
    url: "https://www.facebook.com/groups/1654316948220005/",
    note: "先掃在地修繕題，再挑最值得導流的貼文。",
  },
  {
    id: "group-679286528824529",
    alias: "主社團 02",
    groupId: "679286528824529",
    url: "https://www.facebook.com/groups/679286528824529",
    note: "適合補第二輪搜尋，避免只集中在單一社團。",
  },
  {
    id: "group-163175389244849",
    alias: "主社團 03",
    groupId: "163175389244849",
    url: "https://www.facebook.com/groups/163175389244849",
    note: "當日沒好題時，也可先看留言區是否有待回答問題。",
  },
];

const defaultArticles = [
  {
    id: "soft-furnishing-sequence",
    title: "軟裝進場順序",
    status: "published",
    url: "",
    summary: "你目前已完成的一篇痛點文，可作為回覆導流素材。",
  },
  {
    id: "next-pain-point",
    title: "下一篇痛點文",
    status: "draft",
    url: "",
    summary: "先留給下一篇，例如老屋翻新踩雷、收納尺寸或預算分配。",
  },
];

const defaultGroupsJson = JSON.stringify(defaultGroups);
const defaultArticlesJson = JSON.stringify(defaultArticles);
---

<BaseLayout
  title="社群作戰台 - 青曦空間設計"
  description="青曦內部使用的社群誘餌式回覆與知識導流作戰頁，採遊戲化任務面板設計。"
  noindex={true}
>
  <section class="social-ops-page">
    <div class="social-ops-shell">
      <header class="ops-hero">
        <div class="hero-copy">
          <p class="ops-eyebrow">SOCIAL OPS COMMAND</p>
          <h1>CEO 社群作戰台</h1>
          <p class="ops-description">
            這不是待辦清單，而是每週固定推進的任務面板。打開後直接選任務、進場回覆、完成後劃掉，讓導流像解主線一樣穩定推進。
          </p>
          <div class="hero-actions">
            <a href="#quest-board" class="btn btn-primary">開始本週任務</a>
            <a href="#portal-grid" class="btn btn-secondary">前往社團傳送門</a>
            <button type="button" id="reset-social-ops" class="btn btn-secondary btn-muted">重設本週任務</button>
          </div>
          <p id="ops-feedback" class="ops-feedback" aria-live="polite"></p>
        </div>

        <div class="hero-command-panel">
          <div class="command-badge">
            <span>當前職階</span>
            <strong id="rank-title">Apprentice Operator</strong>
            <small id="level-value">Lv.1</small>
          </div>
          <div class="command-metrics">
            <article class="metric-card metric-card-primary">
              <span>本週主線</span>
              <strong id="weekly-progress-count">0 / 3</strong>
              <small id="weekly-progress-note">距離本週達標還差 3 次出擊</small>
              <div class="progress-track">
                <div id="weekly-progress-bar" class="progress-bar"></div>
              </div>
            </article>
            <article class="metric-card">
              <span>總 XP</span>
              <strong id="xp-value">0 XP</strong>
              <small>每完成一個任務自動累積</small>
            </article>
            <article class="metric-card">
              <span>連勝週數</span>
              <strong id="streak-value">0</strong>
              <small>每週達標 3 次才算延續</small>
            </article>
            <article class="metric-card">
              <span>累積通關</span>
              <strong id="lifetime-runs-value">0</strong>
              <small>本頁只算完整完成的任務數</small>
            </article>
          </div>
        </div>
      </header>

      <div class="ops-grid">
        <div class="ops-main">
          <section id="quest-board" class="ops-panel quest-board">
            <div class="panel-heading">
              <div>
                <p class="panel-tag">WEEKLY QUESTS</p>
                <h2>本週主線任務</h2>
              </div>
              <p>每個任務只做三件事：找到題目、給專業建議、帶痛點文導流。完成就劃掉。</p>
            </div>
            <div id="quest-grid" class="quest-grid"></div>
          </section>

          <section id="portal-grid" class="ops-panel portal-board">
            <div class="panel-heading">
              <div>
                <p class="panel-tag">GROUP PORTALS</p>
                <h2>社團傳送門</h2>
              </div>
              <p>不再用會被瀏覽器擋掉的多開模式。每個社團直接給你首頁、南港、汐止三種入口。</p>
            </div>
            <div id="portal-card-grid" class="portal-card-grid"></div>
          </section>
        </div>

        <aside class="ops-sidebar">
          <section class="ops-panel side-panel">
            <div class="panel-heading compact-heading">
              <div>
                <p class="panel-tag">PLAYBOOK</p>
                <h2>本局打法</h2>
              </div>
            </div>
            <ul class="command-list">
              <li>先進搜尋結果，挑最容易切進專業建議的一題。</li>
              <li>先解決對方眼前問題，再貼最相近的痛點文。</li>
              <li>完成後直接回來打勾，不要再額外填很多字。</li>
            </ul>
          </section>

          <section class="ops-panel side-panel">
            <div class="panel-heading compact-heading">
              <div>
                <p class="panel-tag">ARSENAL</p>
                <h2>導流武器庫</h2>
              </div>
            </div>
            <div id="article-stack" class="article-stack"></div>
          </section>

          <section class="ops-panel side-panel">
            <div class="panel-heading compact-heading">
              <div>
                <p class="panel-tag">REWARDS</p>
                <h2>升級規則</h2>
              </div>
            </div>
            <ul class="command-list reward-list">
              <li>完成 1 個任務：+100 XP</li>
              <li>本週 3 個任務全清：額外 +50 XP</li>
              <li>下週繼續達標：連勝週數 +1</li>
            </ul>
          </section>
        </aside>
      </div>
    </div>
  </section>
</BaseLayout>

<script is:inline define:vars={{ defaultGroupsJson, defaultArticlesJson }}>
  const STORAGE_KEY = "qingxi-social-ops-v2";
  const WEEKLY_TARGET = 3;
  const RUN_XP = 100;
  const WEEKLY_BONUS_XP = 50;
  const groupTemplate = JSON.parse(defaultGroupsJson);
  const articleTemplate = JSON.parse(defaultArticlesJson);
  const rankTitles = [
    "Apprentice Operator",
    "Lead Hunter",
    "Signal Captain",
    "Market Commander",
    "Pipeline CEO",
    "Empire Architect",
  ];

  function getWeekKey(date = new Date()) {
    const target = new Date(date);
    const day = target.getDay() || 7;
    target.setHours(0, 0, 0, 0);
    target.setDate(target.getDate() + 4 - day);
    const yearStart = new Date(target.getFullYear(), 0, 1);
    const weekNo = Math.ceil((((target - yearStart) / 86400000) + 1) / 7);
    return `${target.getFullYear()}-W${String(weekNo).padStart(2, "0")}`;
  }

  function createRun(slot, keyword, title, guidance) {
    return {
      id: `run-${slot}`,
      slot,
      keyword,
      title,
      guidance,
      actionSearch: false,
      actionAdvice: false,
      actionArticle: false,
      rewardClaimed: false,
      completedAt: "",
    };
  }

  function createWeeklyRuns() {
    return [
      createRun(1, "南港", "Quest 01｜南港主線", "先解在地修繕問題，再附最貼近的痛點文。"),
      createRun(2, "汐止", "Quest 02｜汐止主線", "選最容易切進專業判斷的一題，快速完成。"),
      createRun(3, "自由掃場", "Quest 03｜自由補刀", "自由挑一題最值得導流的貼文，不限南港或汐止。"),
    ];
  }

  function createInitialState() {
    return {
      weekly: {
        weekKey: getWeekKey(),
        bonusClaimed: false,
        runs: createWeeklyRuns(),
      },
      stats: {
        xp: 0,
        streak: 0,
        lifetimeRuns: 0,
      },
    };
  }

  let state = createInitialState();

  function saveState() {
    localStorage.setItem(STORAGE_KEY, JSON.stringify(state));
  }

  function loadState() {
    try {
      const raw = localStorage.getItem(STORAGE_KEY);
      if (!raw) return;
      const parsed = JSON.parse(raw);
      state = {
        weekly: parsed.weekly || createInitialState().weekly,
        stats: {
          ...createInitialState().stats,
          ...(parsed.stats || {}),
        },
      };
    } catch (error) {
      console.error("讀取社群作戰台資料失敗：", error);
    }
  }

  function isRunComplete(run) {
    return run.actionSearch && run.actionAdvice && run.actionArticle;
  }

  function normalizeWeeklyState() {
    const currentWeekKey = getWeekKey();
    if (state.weekly.weekKey === currentWeekKey) return;

    const completedRuns = state.weekly.runs.filter(isRunComplete).length;
    state.stats.streak = completedRuns >= WEEKLY_TARGET ? state.stats.streak + 1 : 0;
    state.weekly = {
      weekKey: currentWeekKey,
      bonusClaimed: false,
      runs: createWeeklyRuns(),
    };
    saveState();
  }

  function escapeHtml(value) {
    return String(value ?? "")
      .replace(/&/g, "&amp;")
      .replace(/</g, "&lt;")
      .replace(/>/g, "&gt;")
      .replace(/"/g, "&quot;")
      .replace(/'/g, "&#39;");
  }

  function setFeedback(message, mode = "") {
    const shell = document.getElementById("ops-feedback");
    if (!shell) return;
    shell.className = `ops-feedback ${mode}`.trim();
    shell.textContent = message;
  }

  function buildSearchUrl(group, keyword) {
    return `https://www.facebook.com/groups/${encodeURIComponent(group.groupId)}/search/?q=${encodeURIComponent(keyword)}`;
  }

  function getRunPortals(run) {
    if (run.keyword === "南港" || run.keyword === "汐止") {
      return groupTemplate.map((group) => ({
        label: `${group.alias} ${run.keyword}`,
        url: buildSearchUrl(group, run.keyword),
      }));
    }

    return groupTemplate.map((group) => ({
      label: `${group.alias} 首頁`,
      url: group.url,
    }));
  }

  function getCompletedRunCount() {
    return state.weekly.runs.filter(isRunComplete).length;
  }

  function getLevel() {
    return Math.floor(state.stats.xp / 200) + 1;
  }

  function getRankTitle(level) {
    return rankTitles[Math.min(level - 1, rankTitles.length - 1)];
  }

  function maybeClaimRunReward(run) {
    if (!isRunComplete(run) || run.rewardClaimed) return;
    run.rewardClaimed = true;
    run.completedAt = new Date().toISOString();
    state.stats.xp += RUN_XP;
    state.stats.lifetimeRuns += 1;

    const completedRuns = getCompletedRunCount();
    if (completedRuns >= WEEKLY_TARGET && !state.weekly.bonusClaimed) {
      state.weekly.bonusClaimed = true;
      state.stats.xp += WEEKLY_BONUS_XP;
      setFeedback("本週三個任務已全清，已領取 +50 XP 額外獎勵。", "is-success");
    }
  }

  function renderHero() {
    const completedRuns = getCompletedRunCount();
    const remaining = Math.max(WEEKLY_TARGET - completedRuns, 0);
    const percent = Math.round((completedRuns / WEEKLY_TARGET) * 100);
    const level = getLevel();
    document.getElementById("weekly-progress-count").textContent = `${completedRuns} / ${WEEKLY_TARGET}`;
    document.getElementById("weekly-progress-note").textContent = remaining === 0
      ? "本週主線已清空，維持連勝節奏。"
      : `距離本週達標還差 ${remaining} 個任務`;
    document.getElementById("weekly-progress-bar").style.width = `${percent}%`;
    document.getElementById("xp-value").textContent = `${state.stats.xp} XP`;
    document.getElementById("streak-value").textContent = String(state.stats.streak);
    document.getElementById("lifetime-runs-value").textContent = String(state.stats.lifetimeRuns);
    document.getElementById("level-value").textContent = `Lv.${level}`;
    document.getElementById("rank-title").textContent = getRankTitle(level);
  }

  function renderQuests() {
    const shell = document.getElementById("quest-grid");
    if (!shell) return;

    shell.innerHTML = state.weekly.runs
      .map((run, index) => {
        const complete = isRunComplete(run);
        const portals = getRunPortals(run)
          .map((portal) => `<a class="portal-chip" href="${escapeHtml(portal.url)}" target="_blank" rel="noreferrer">${escapeHtml(portal.label)}</a>`)
          .join("");

        return `
          <article class="quest-card ${complete ? "is-complete" : ""}">
            <div class="quest-topline">
              <span class="quest-slot">RUN ${run.slot}</span>
              <span class="quest-keyword">${escapeHtml(run.keyword)}</span>
            </div>
            <h3>${escapeHtml(run.title)}</h3>
            <p class="quest-guidance">${escapeHtml(run.guidance)}</p>
            <div class="quest-checklist">
              <label class="quest-check ${run.actionSearch ? "is-checked" : ""}">
                <input type="checkbox" data-run-index="${index}" data-run-check="actionSearch" ${run.actionSearch ? "checked" : ""} />
                <span>已進場找題</span>
              </label>
              <label class="quest-check ${run.actionAdvice ? "is-checked" : ""}">
                <input type="checkbox" data-run-index="${index}" data-run-check="actionAdvice" ${run.actionAdvice ? "checked" : ""} />
                <span>已給專業建議</span>
              </label>
              <label class="quest-check ${run.actionArticle ? "is-checked" : ""}">
                <input type="checkbox" data-run-index="${index}" data-run-check="actionArticle" ${run.actionArticle ? "checked" : ""} />
                <span>已附痛點文導流</span>
              </label>
            </div>
            <div class="quest-portals">${portals}</div>
            <div class="quest-footer">
              <small>${complete ? `CLEAR：${new Date(run.completedAt).toLocaleString("zh-TW")}` : "清空三格後自動獲得 +100 XP"}</small>
              <strong>${complete ? "Quest Cleared" : "Quest Active"}</strong>
            </div>
          </article>
        `;
      })
      .join("");
  }

  function renderPortals() {
    const shell = document.getElementById("portal-card-grid");
    if (!shell) return;

    shell.innerHTML = groupTemplate
      .map((group) => `
        <article class="portal-card">
          <div class="portal-card-top">
            <span class="portal-card-label">${escapeHtml(group.alias)}</span>
            <h3>${escapeHtml(group.groupId)}</h3>
          </div>
          <p>${escapeHtml(group.note)}</p>
          <div class="portal-actions">
            <a class="portal-link primary-link" href="${escapeHtml(group.url)}" target="_blank" rel="noreferrer">社團首頁</a>
            <a class="portal-link" href="${escapeHtml(buildSearchUrl(group, "南港"))}" target="_blank" rel="noreferrer">搜南港</a>
            <a class="portal-link" href="${escapeHtml(buildSearchUrl(group, "汐止"))}" target="_blank" rel="noreferrer">搜汐止</a>
          </div>
        </article>
      `)
      .join("");
  }

  function renderArticles() {
    const shell = document.getElementById("article-stack");
    if (!shell) return;

    shell.innerHTML = articleTemplate
      .map((article) => `
        <article class="article-card ${article.status === "published" ? "is-published" : ""}">
          <div class="article-head">
            <span class="article-state">${article.status === "published" ? "READY" : "DRAFT"}</span>
            <h3>${escapeHtml(article.title)}</h3>
          </div>
          <p>${escapeHtml(article.summary)}</p>
          ${article.url ? `<a class="article-link" href="${escapeHtml(article.url)}" target="_blank" rel="noreferrer">開啟素材</a>` : `<span class="article-link is-muted">連結待補</span>`}
        </article>
      `)
      .join("");
  }

  function rerender() {
    renderHero();
    renderQuests();
    renderPortals();
    renderArticles();
    saveState();
  }

  document.addEventListener("change", (event) => {
    const target = event.target;
    if (!(target instanceof HTMLInputElement)) return;
    if (!target.dataset.runCheck) return;

    const index = Number(target.dataset.runIndex);
    const key = target.dataset.runCheck;
    const run = state.weekly.runs[index];
    if (!run || !key) return;

    run[key] = target.checked;
    maybeClaimRunReward(run);
    rerender();
  });

  document.getElementById("reset-social-ops")?.addEventListener("click", () => {
    state.weekly = {
      weekKey: getWeekKey(),
      bonusClaimed: false,
      runs: createWeeklyRuns(),
    };
    rerender();
    setFeedback("本週任務已重設，XP 與連勝保留。", "is-warning");
  });

  loadState();
  normalizeWeeklyState();
  rerender();
</script>

<style>
  .social-ops-page {
    padding: 5rem 1.25rem 6rem;
    background:
      radial-gradient(circle at top, rgba(120, 168, 255, 0.16), transparent 24%),
      radial-gradient(circle at right top, rgba(80, 226, 205, 0.12), transparent 28%),
      linear-gradient(180deg, #060812 0%, #0b1020 44%, #0d1426 100%);
    color: #eff4ff;
  }

  .social-ops-shell {
    max-width: 1320px;
    margin: 0 auto;
    display: grid;
    gap: 1.5rem;
  }

  .ops-hero,
  .ops-panel {
    border-radius: 30px;
    border: 1px solid rgba(140, 168, 255, 0.16);
    background:
      linear-gradient(180deg, rgba(15, 21, 39, 0.94), rgba(10, 15, 29, 0.92));
    box-shadow:
      0 30px 80px rgba(0, 0, 0, 0.34),
      inset 0 1px 0 rgba(255, 255, 255, 0.04);
    backdrop-filter: blur(18px);
  }

  .ops-hero {
    padding: clamp(1.5rem, 3vw, 2.4rem);
    display: grid;
    gap: 1.5rem;
    grid-template-columns: minmax(0, 1.35fr) minmax(320px, 0.9fr);
  }

  .ops-eyebrow,
  .panel-tag,
  .quest-slot,
  .portal-card-label,
  .article-state {
    margin: 0;
    letter-spacing: 0.18em;
    font-size: 0.74rem;
    color: #87f5dd;
  }

  .hero-copy h1 {
    margin: 0;
    font-family: "Noto Serif TC", serif;
    font-size: clamp(2.3rem, 4vw, 3.8rem);
    line-height: 1.08;
    color: #ffffff;
  }

  .ops-description {
    margin: 1rem 0 0;
    max-width: 720px;
    color: rgba(231, 238, 255, 0.78);
    font-size: 1.02rem;
    line-height: 1.9;
  }

  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 0.8rem;
    margin-top: 1.4rem;
  }

  .ops-feedback {
    min-height: 1.5rem;
    margin: 0.95rem 0 0;
    color: rgba(231, 238, 255, 0.76);
  }

  .ops-feedback.is-success {
    color: #82ffd4;
  }

  .ops-feedback.is-warning {
    color: #ffd88a;
  }

  .hero-command-panel,
  .command-metrics,
  .ops-grid,
  .ops-main,
  .ops-sidebar,
  .quest-grid,
  .portal-card-grid,
  .article-stack {
    display: grid;
    gap: 1rem;
  }

  .command-badge,
  .metric-card,
  .quest-card,
  .portal-card,
  .article-card {
    border-radius: 24px;
    border: 1px solid rgba(140, 168, 255, 0.14);
    background:
      linear-gradient(180deg, rgba(17, 24, 44, 0.92), rgba(10, 16, 30, 0.94));
    box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.03);
  }

  .command-badge {
    padding: 1.2rem 1.2rem 1rem;
    background:
      radial-gradient(circle at top left, rgba(102, 167, 255, 0.22), transparent 42%),
      linear-gradient(180deg, rgba(21, 31, 58, 0.98), rgba(12, 18, 34, 0.96));
  }

  .command-badge span,
  .metric-card span,
  .metric-card small,
  .quest-guidance,
  .quest-footer small,
  .portal-card p,
  .article-card p,
  .panel-heading p,
  .command-list li {
    color: rgba(224, 232, 255, 0.72);
  }

  .command-badge strong {
    display: block;
    margin-top: 0.45rem;
    font-size: 1.52rem;
    color: #ffffff;
  }

  .command-badge small {
    display: inline-flex;
    margin-top: 0.45rem;
    color: #87f5dd;
  }

  .command-metrics {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .metric-card {
    padding: 1rem 1.05rem;
  }

  .metric-card strong {
    display: block;
    margin-top: 0.32rem;
    font-size: 1.55rem;
    color: #ffffff;
  }

  .metric-card-primary {
    grid-column: 1 / -1;
    background:
      radial-gradient(circle at top right, rgba(135, 245, 221, 0.16), transparent 38%),
      linear-gradient(180deg, rgba(18, 28, 54, 0.98), rgba(11, 17, 33, 0.96));
  }

  .progress-track {
    margin-top: 0.85rem;
    height: 8px;
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.08);
    overflow: hidden;
  }

  .progress-bar {
    width: 0;
    height: 100%;
    border-radius: inherit;
    background: linear-gradient(90deg, #6fe2ff, #87f5dd);
    transition: width 0.35s ease;
  }

  .ops-grid {
    grid-template-columns: minmax(0, 1.42fr) minmax(300px, 0.7fr);
    align-items: start;
  }

  .ops-panel {
    padding: 1.35rem;
  }

  .panel-heading {
    display: flex;
    justify-content: space-between;
    gap: 1rem;
    align-items: end;
    margin-bottom: 1.15rem;
  }

  .compact-heading {
    margin-bottom: 0.95rem;
  }

  .panel-heading h2 {
    margin: 0.2rem 0 0;
    font-family: "Noto Serif TC", serif;
    font-size: 1.55rem;
    color: #ffffff;
  }

  .panel-heading p {
    margin: 0;
    max-width: 360px;
    line-height: 1.8;
  }

  .quest-grid,
  .portal-card-grid {
    grid-template-columns: repeat(3, minmax(0, 1fr));
  }

  .quest-card,
  .portal-card,
  .article-card {
    padding: 1rem;
  }

  .quest-card {
    position: relative;
    overflow: hidden;
  }

  .quest-card::before {
    content: "";
    position: absolute;
    inset: 0;
    background: radial-gradient(circle at top right, rgba(102, 167, 255, 0.14), transparent 30%);
    pointer-events: none;
  }

  .quest-card.is-complete {
    border-color: rgba(135, 245, 221, 0.28);
    background:
      radial-gradient(circle at top right, rgba(135, 245, 221, 0.1), transparent 34%),
      linear-gradient(180deg, rgba(15, 30, 34, 0.96), rgba(10, 19, 24, 0.96));
  }

  .quest-topline,
  .quest-portals,
  .portal-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 0.65rem;
  }

  .quest-topline {
    justify-content: space-between;
    align-items: center;
  }

  .quest-keyword {
    display: inline-flex;
    padding: 0.34rem 0.7rem;
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.08);
    color: #c7d7ff;
    font-size: 0.82rem;
  }

  .quest-card h3,
  .portal-card h3,
  .article-card h3 {
    margin: 0.7rem 0 0;
    color: #ffffff;
    font-size: 1.2rem;
  }

  .quest-guidance {
    margin: 0.72rem 0 0;
    line-height: 1.75;
  }

  .quest-checklist {
    display: grid;
    gap: 0.7rem;
    margin-top: 1rem;
  }

  .quest-check {
    display: flex;
    align-items: center;
    gap: 0.7rem;
    padding: 0.78rem 0.88rem;
    border-radius: 16px;
    background: rgba(255, 255, 255, 0.04);
    color: #f0f5ff;
    transition: background 0.25s ease;
  }

  .quest-check.is-checked {
    background: rgba(135, 245, 221, 0.12);
    color: rgba(240, 245, 255, 0.76);
  }

  .quest-check.is-checked span {
    text-decoration: line-through;
    text-decoration-thickness: 2px;
  }

  .quest-check input[type="checkbox"] {
    width: 18px;
    height: 18px;
    accent-color: #87f5dd;
  }

  .quest-portals {
    margin-top: 1rem;
  }

  .portal-chip,
  .portal-link,
  .article-link {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 44px;
    padding: 0.7rem 0.95rem;
    border-radius: 14px;
    border: 1px solid rgba(140, 168, 255, 0.18);
    background: rgba(255, 255, 255, 0.04);
    color: #e9f1ff;
    text-decoration: none;
    transition: transform 0.25s ease, border-color 0.25s ease, background 0.25s ease;
  }

  .portal-chip:hover,
  .portal-link:hover,
  .article-link:hover {
    transform: translateY(-2px);
    border-color: rgba(135, 245, 221, 0.34);
    background: rgba(135, 245, 221, 0.08);
  }

  .quest-footer {
    display: flex;
    justify-content: space-between;
    gap: 1rem;
    align-items: center;
    margin-top: 1rem;
    padding-top: 0.95rem;
    border-top: 1px solid rgba(255, 255, 255, 0.08);
  }

  .quest-footer strong {
    color: #87f5dd;
    letter-spacing: 0.08em;
    font-size: 0.82rem;
  }

  .portal-card {
    display: grid;
    gap: 0.8rem;
  }

  .portal-card-top {
    display: grid;
    gap: 0.2rem;
  }

  .portal-card h3 {
    font-size: 1rem;
    color: #d9e5ff;
    word-break: break-all;
  }

  .portal-card p,
  .article-card p,
  .command-list li {
    margin: 0;
    line-height: 1.75;
  }

  .portal-actions {
    margin-top: 0.15rem;
  }

  .primary-link {
    background: linear-gradient(135deg, rgba(102, 167, 255, 0.16), rgba(135, 245, 221, 0.1));
  }

  .article-stack {
    gap: 0.85rem;
  }

  .article-card {
    display: grid;
    gap: 0.8rem;
  }

  .article-link.is-muted {
    border-style: dashed;
    color: rgba(231, 238, 255, 0.58);
    pointer-events: none;
  }

  .command-list {
    margin: 0;
    padding-left: 1.15rem;
  }

  @media (max-width: 1120px) {
    .ops-hero,
    .ops-grid,
    .quest-grid,
    .portal-card-grid {
      grid-template-columns: 1fr;
    }

    .command-metrics {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }

  @media (max-width: 720px) {
    .social-ops-page {
      padding-inline: 1rem;
    }

    .hero-actions,
    .panel-heading,
    .quest-footer,
    .quest-portals,
    .portal-actions {
      flex-direction: column;
      align-items: stretch;
    }

    .command-metrics {
      grid-template-columns: 1fr;
    }
  }
</style>
